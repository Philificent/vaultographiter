# Absolute path to an existing Obsidian vault directory (required in practice).
# Default when unset: ~/Documents/Obsidian
VAULT_PATH=/Users/you/Documents/Obsidian

# Server port (optional). The Vite dev proxy expects 4400.
PORT=4400
